---
name: release
description: Cut a new release of @ffmpeg-micro/mcp-server. Use when the user says "cut a release", "release X.Y.Z", "ship a new version", "publish a patch/minor/major".
---

# Release

The release process is documented in the repo README — treat it as the source of truth. Read `README.md` (the "Release process" section) before cutting a release, and follow the steps there verbatim.

If anything about the release process needs to change (new steps, new checks, new infra), **update `README.md`, not this file.** This skill exists only to route release requests to the README.
